<Window x:Class="LogViewer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogViewer.App"
        mc:Ignorable="d"
        Title="LogViewer" Height="700" Width="1200"
        AllowDrop="True" Drop="Window_Drop" DragOver="Window_DragOver">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Open..." Click="OpenButton_Click" Padding="8,2" ToolTip="Open one or more log files (Ctrl+O)"/>
        </ToolBar>
        <TabControl x:Name="Tabs" DockPanel.Dock="Top">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <Button Content="✕" Padding="4,0" FontSize="10"
                                            Click="CloseTabButton_Click"
                                            Tag="{Binding RelativeSource={RelativeSource AncestorType=TabItem}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>
    </DockPanel>
</Window>
